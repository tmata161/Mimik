# Project Mimik

# Build guide
cmake -B build

# Run guide
make -j

# List of commands


repair usb unmounting errors[only working on linux; but still error on windows]
add icons[done]
add a new bootlogo [done]
xip execution[no need; done using __in_flash() attribute)
add REPEAT instruction [done]
setup interrupt to abrupt removal of memory card
fix some errors in payload parser